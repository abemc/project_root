# テスト実行サマリ

日付: 2026-05-11

簡潔な結果:

- `torch` を `>=2.6.0` に更新し、仮想環境に CPU ビルドをインストールしました（確認済み: `torch 2.11.0+cpu`）。
- `USE_MOCK_VISION=1` モードを用いることで CI/ローカルのユニットテストが安定し、長時間のモデルダウンロードを回避できます。
- ローカルでフィルタ済テストを実行した結果: 912 passed, 181 deselected（詳細はテストログ参照）。

次の提案:

- CI ジョブに `USE_MOCK_VISION=1` を適用（このリポジトリにワークフロー追加済み）。
- 定期的にフルモデル検証用のジョブを別途用意し、モデルの互換性や精度の回帰をチェック。
