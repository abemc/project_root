このリポジトリはユーザーの指示により Git 管理を停止しました。

- 元の `.git` ディレクトリはバックアップされました: `.git.backup-20260517153858`（タイムスタンプは変更される場合があります）。
- Git 機能を復元するには、バックアップをリネームして `.git` に戻してください:

  mv .git.backup-20260517153858 .git

- 今後はローカルでファイルを管理してください。必要であれば `git init` で新たにリポジトリを作成できます。

作成日時: 2026-05-17
