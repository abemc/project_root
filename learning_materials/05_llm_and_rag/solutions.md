# 演習 解答例（概要）

1) 埋め込みの作成と検索
- `sentence-transformers` の `all-MiniLM-L6-v2` などを使い、文ごとの埋め込みを作成します。コサイン類似度でソートして上位 k を返します。

2) LLM との統合
- 検索結果を短い箇条書きにし、`質問:` と合わせて LLM に渡します。過度に長い文は要約して渡すとよいです。

3) Faiss でスケール
- Faiss の IndexFlatIP を用い、正規化した埋め込みで内積検索（コサイン近似）を行います。インデックスは disk に保存して再利用可能にします。
